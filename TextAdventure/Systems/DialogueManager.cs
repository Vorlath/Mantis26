namespace TextAdventure.Systems
{
    public class DialogueManager
    {
        DialogueNode CurrentNode;

        public DialogueManager(DialogueNode initNode)
        {
            this.CurrentNode = initNode;
        }

        // This should generate the dialogue, and make decisions on 


    }
}
